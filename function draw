// usando map() para ajustar a cor de fundo de forma mais controlada
  let corFundo = lerpColor(color(217,112,26), color(219,239,208),
  map(totalarvores, 0,100,0,1));
  background(corFundo);
  mostrarInformaçoẽs();
  temperatura += 0.1;
  jardineiro.atualizar();
  jardineiro.mostrar();
 
  verificarF
  //verifica se o jogo acabouimDeJogo();
 
